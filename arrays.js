// Cores

let cores = ['azul', 'amarelo', 'verde', 'laranja', 'vermelho']

console.log(cores)

// chamar uma cor específica
console.log(cores[2])

// adicionar item novo ao array
// primeiro vamos consultar o tamanho do array
console.log(cores.length)

cores[5] = 'rosa'
console.log(cores)